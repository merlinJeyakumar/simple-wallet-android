<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/login_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:id="@+id/login_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:fillViewport="true"
        android:overScrollMode="ifContentScrolls">

    <LinearLayout
        android:id="@+id/login_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:paddingStart="@dimen/wallet_screen_margin"
        android:paddingTop="@dimen/wallet_space_32"
        android:paddingEnd="@dimen/wallet_screen_margin"
        android:paddingBottom="@dimen/wallet_space_32">

        <ImageView
            android:id="@+id/login_logo"
            android:layout_width="@dimen/wallet_logo_size"
            android:layout_height="@dimen/wallet_logo_size"
            android:contentDescription="@string/login_logo_description"
            android:src="@drawable/ic_wallet_app" />

        <TextView
            android:id="@+id/login_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/wallet_space_24"
            android:text="@string/login_title"
            android:textAppearance="@style/TextAppearance.SimpleWallet.Headline" />

        <TextView
            android:id="@+id/login_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/wallet_space_8"
            android:text="@string/login_subtitle"
            android:textAppearance="@style/TextAppearance.SimpleWallet.Supporting" />

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/login_identifier_layout"
            style="@style/Widget.SimpleWallet.TextInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/wallet_space_32"
            android:hint="@string/login_identifier_label"
            app:errorEnabled="true">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/login_identifier_input"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:autofillHints="username"
                android:imeOptions="actionNext"
                android:inputType="textEmailAddress"
                android:maxLines="1" />
        </com.google.android.material.textfield.TextInputLayout>

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/login_password_layout"
            style="@style/Widget.SimpleWallet.TextInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/wallet_space_8"
            android:hint="@string/login_password_label"
            app:endIconMode="password_toggle"
            app:errorEnabled="true">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/login_password_input"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:autofillHints="password"
                android:imeOptions="actionDone"
                android:inputType="textPassword"
                android:maxLines="1" />
        </com.google.android.material.textfield.TextInputLayout>

        <TextView
            android:id="@+id/login_error_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/wallet_space_8"
            android:accessibilityLiveRegion="polite"
            android:textColor="@color/wallet_error"
            android:textSize="@dimen/wallet_text_label"
            android:visibility="gone" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/login_button"
            style="@style/Widget.SimpleWallet.PrimaryButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/wallet_space_16"
            android:text="@string/login_action" />

    </LinearLayout>
    </ScrollView>

    <LinearLayout
        android:id="@+id/login_progress"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:accessibilityLiveRegion="polite"
        android:background="@color/wallet_background"
        android:clickable="true"
        android:contentDescription="@string/login_loading"
        android:elevation="@dimen/wallet_space_8"
        android:focusable="true"
        android:gravity="center"
        android:importantForAccessibility="yes"
        android:orientation="vertical"
        android:padding="@dimen/wallet_space_24"
        android:visibility="gone">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/login_progress_indicator"
            style="@style/Widget.SimpleWallet.LoadingIndicator"
            android:layout_width="@dimen/wallet_touch_target"
            android:layout_height="@dimen/wallet_touch_target" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/wallet_space_16"
            android:text="@string/login_loading"
            android:textAppearance="@style/TextAppearance.SimpleWallet.Supporting" />
    </LinearLayout>
</FrameLayout>
